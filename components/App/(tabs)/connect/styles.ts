import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  reactLogo: {
    height: 200,
    width: 290,
    bottom: 0,
    left: 0,
    position: "absolute",
  },
  input: {
    borderWidth: 1,
    borderColor: "#ccc",
    padding: 8,
    borderRadius: 4,
  },
});
